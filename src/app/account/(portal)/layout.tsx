import PortalShell from "@/components/portal/PortalShell";
import { requireCustomer } from "@/lib/auth/guards";
import { isFeatureEnabled } from "@/lib/config/featureFlags";
export const dynamic = "force-dynamic";
export default async function AccountPortalLayout({ children }: { children: React.ReactNode }) { const { user, supabase } = await requireCustomer("/account"); const { data: profile } = await supabase.from("profiles").select("first_name, last_name").eq("id", user.id).maybeSingle(); const nav = [{ href: "/account/orders", label: "Orders" }, ...(isFeatureEnabled("CLOUD_DESIGNS_ENABLED") ? [{ href: "/account/designs", label: "Saved designs" }] : []), { href: "/account/billing", label: "Billing & addresses" }, { href: "/account/privacy", label: "Privacy" }]; const customerName = `${profile?.first_name ?? "Customer"} ${profile?.last_name ?? ""}`.trim(); return <PortalShell kind="Customer workspace" title={customerName} subtitle="Your private Garmops account" identity={user.email ?? user.id} nav={nav}>{children}</PortalShell>; }
