import Image from "next/image";
import Link from "next/link";
import { ArrowRight, Palette, Plus } from "lucide-react";
import { requireOrganizationMember } from "@/lib/auth/guards";
import { isFeatureEnabled } from "@/lib/config/featureFlags";
import { listCloudDesigns } from "@/lib/designs/dal";
import { cloudDesignSnapshotSchema, type CloudDesignSnapshot } from "@/lib/designs/schema";
import { getProduct } from "@/lib/configurator/products";
import { formatOrderTimestamp } from "@/lib/orders/format";
import BrowserDraftImporter from "@/components/account/BrowserDraftImporter";

export const dynamic = "force-dynamic";
type DesignEntry = { design: NonNullable<Awaited<ReturnType<typeof listCloudDesigns>>["data"]>[number]; snapshot: CloudDesignSnapshot };

export default async function SavedDesignsPage({ searchParams }: { searchParams: Promise<{ filter?: string; q?: string }> }) {
  const { filter = "active", q = "" } = await searchParams;
  const { supabase, user } = await requireOrganizationMember("/account/designs");
  if (!isFeatureEnabled("CLOUD_DESIGNS_ENABLED")) return <div className="rounded-[4px] border border-dashed p-10 text-center"><h1 className="text-2xl font-semibold">Saved designs are not available</h1><p className="mx-auto mt-2 max-w-md text-sm text-black/50">Start a design from the configurator and save it to your account when this feature is enabled.</p><Link href="/configurator" className="mt-6 inline-flex rounded-[4px] bg-[var(--color-accent)] px-4 py-2.5 text-sm font-semibold text-white">Start designing</Link></div>;
  const { data, error } = await listCloudDesigns(supabase, user.id, filter === "archived");
  if (error) return <div className="rounded-[4px] border border-dashed p-10 text-center"><h1 className="text-2xl font-semibold">Saved designs unavailable</h1><p className="mt-2 text-sm text-black/50">We couldn’t load your saved designs. Refresh the page or try again shortly.</p></div>;
  const designs: DesignEntry[] = (data ?? []).filter((design) => filter === "archived" ? design.status === "archived" : design.status !== "archived").map((design) => { const snapshot = cloudDesignSnapshotSchema.safeParse(design.draft_snapshot); return snapshot.success ? { design, snapshot: snapshot.data } : null; }).filter((entry): entry is DesignEntry => entry !== null).filter(({ design, snapshot }) => { const product = getProduct(snapshot.configId); return !q.trim() || `${design.title} ${product?.name ?? snapshot.configId}`.toLowerCase().includes(q.trim().toLowerCase()); });
  return <div className="space-y-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs uppercase tracking-[0.18em] text-[var(--color-accent)]">Account</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Saved designs</h1><p className="mt-2 max-w-xl text-sm leading-relaxed text-black/50">Open a saved design to review or continue editing it.</p></div><Link href="/configurator" className="inline-flex items-center justify-center gap-2 rounded-[4px] bg-[var(--color-accent)] px-4 py-2.5 text-sm font-semibold text-white hover:bg-[var(--color-accent-dark)]"><Plus size={16} /> Start a new design</Link></div><BrowserDraftImporter /><div className="flex flex-col gap-3 border-b border-black/8 pb-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-2"><Link href="/account/designs?filter=active" className={`rounded-[4px] px-3 py-1.5 text-xs font-semibold ${filter !== "archived" ? "bg-[var(--color-accent)] text-white" : "bg-black/5 text-black/55"}`}>Active</Link><Link href="/account/designs?filter=archived" className={`rounded-[4px] px-3 py-1.5 text-xs font-semibold ${filter === "archived" ? "bg-[var(--color-accent)] text-white" : "bg-black/5 text-black/55"}`}>Archived</Link></div><form className="flex gap-2" action="/account/designs"><input type="hidden" name="filter" value={filter} /><label className="sr-only" htmlFor="design-search">Search saved designs</label><input id="design-search" name="q" defaultValue={q} placeholder="Search designs" className="w-full rounded-[4px] border border-black/10 px-3 py-2 text-sm outline-none focus:border-[var(--color-accent)] sm:w-56" /><button className="rounded-[4px] border border-black/10 px-3 py-2 text-sm font-semibold">Search</button></form></div>{designs.length ? <div className="grid gap-4 lg:grid-cols-2">{designs.map(({ design, snapshot }) => { const product = getProduct(snapshot.configId); const configuration = snapshot.configuration; const artworkSides = [configuration.artwork.front ? "Front" : null, configuration.artwork.back ? "Back" : null].filter(Boolean).join(" + "); return <article key={design.id} className="grid gap-4 rounded-[4px] border border-black/8 bg-white p-4 shadow-sm sm:grid-cols-[150px_minmax(0,1fr)] sm:p-5"><Link href={`/account/designs/${encodeURIComponent(design.id)}`} className="group relative flex min-h-40 items-center justify-center overflow-hidden rounded-[4px] bg-[#F4F4F1]" aria-label={`View ${design.title}`}><Image src={product?.defaultImage ?? "/flatlays/regulartee.png"} alt={`${product?.name ?? "Garment"} in ${configuration.colour.name}`} fill sizes="(max-width: 640px) 100vw, 150px" className="object-contain p-4 transition group-hover:scale-105" /></Link><div className="flex min-w-0 flex-col"><h2 className="truncate text-lg font-semibold">{design.title}</h2><p className="mt-1 truncate text-sm text-black/55">{product?.name ?? snapshot.configId}</p><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><p className="text-xs text-black/40">Quantity</p><p className="mt-1 font-medium">{configuration.quantity.toLocaleString("en-IN")} pcs</p></div><div><p className="text-xs text-black/40">Customisation</p><p className="mt-1 font-medium">{artworkSides || "No artwork"}{configuration.neckLabel ? " · Label" : ""}</p></div></div><p className="mt-3 text-xs text-black/45">Last saved {formatOrderTimestamp(design.last_saved_at)}</p><Link href={`/account/designs/${encodeURIComponent(design.id)}`} className="mt-3 inline-flex items-center gap-1 border-t border-black/8 pt-3 text-sm font-semibold text-[var(--color-accent)] hover:underline">View details <ArrowRight size={15} /></Link></div></article>; })}</div> : <div className="rounded-[4px] border border-dashed p-10 text-center"><Palette size={30} className="mx-auto text-[var(--color-accent)]" /><h2 className="mt-4 text-xl font-semibold">No saved designs yet</h2><p className="mx-auto mt-2 max-w-md text-sm text-black/50">Save a design from the configurator to continue it later.</p><Link href="/configurator" className="mt-5 inline-flex rounded-[4px] bg-[var(--color-accent)] px-4 py-2.5 text-sm font-semibold text-white">Start designing</Link></div>}</div>;
}
