import { createPrivacyRequest } from "@/app/account/privacy-actions";
import { requireCustomer } from "@/lib/auth/guards";
export default async function AccountPrivacyPage() { await requireCustomer("/account/privacy"); return <div className="space-y-6"><header><h1 className="text-3xl font-semibold">Privacy controls</h1><p className="mt-2 text-sm text-black/55">Review your data rights and submit a request to the Garmops team.</p></header><form action={createPrivacyRequest} className="techpack-surface space-y-4 rounded border p-5"><label className="block text-sm font-semibold">Request type<select name="requestType" className="mt-1 block w-full rounded border p-2"><option value="export">Export my data</option><option value="correction">Correct my data</option><option value="delete">Delete my account/data</option></select></label><label className="block text-sm font-semibold">Details<textarea name="note" maxLength={2000} rows={4} className="mt-1 block w-full rounded border p-2"/></label><button className="techpack-button">Submit request</button></form></div>; }
