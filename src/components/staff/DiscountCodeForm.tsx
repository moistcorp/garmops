"use client";
import { useActionState } from "react";
import { createDiscountCodeAction } from "@/app/staff/actions";
import { INITIAL_STAFF_ACTION_STATE } from "@/lib/staff/actionState";
export default function DiscountCodeForm() { const [state, action, pending] = useActionState(createDiscountCodeAction, INITIAL_STAFF_ACTION_STATE); return <form action={action} className="space-y-3"><label className="block text-xs font-semibold">Code<input name="code" required placeholder="WELCOME10" className="mt-1 w-full rounded border border-black/10 px-3 py-2 font-mono text-sm uppercase" /></label><label className="block text-xs font-semibold">Description<input name="description" className="mt-1 w-full rounded border border-black/10 px-3 py-2 text-sm" /></label><div className="grid grid-cols-2 gap-3"><label className="block text-xs font-semibold">Type<select name="kind" className="mt-1 w-full rounded border border-black/10 bg-white px-3 py-2 text-sm"><option value="percentage">Percentage</option><option value="fixed">Fixed ₹</option></select></label><label className="block text-xs font-semibold">Value<input name="value" type="number" min="0.01" step="0.01" required className="mt-1 w-full rounded border border-black/10 px-3 py-2 text-sm" /></label></div><label className="block text-xs font-semibold">Minimum subtotal (₹)<input name="minimumSubtotalRupees" type="number" min="0" step="0.01" defaultValue="0" className="mt-1 w-full rounded border border-black/10 px-3 py-2 text-sm" /></label><div className="grid grid-cols-2 gap-3"><label className="block text-xs font-semibold">Max redemptions<input name="maximumRedemptions" type="number" min="1" className="mt-1 w-full rounded border border-black/10 px-3 py-2 text-sm" /></label><label className="block text-xs font-semibold">Per customer<input name="perCustomer" type="number" min="1" defaultValue="1" className="mt-1 w-full rounded border border-black/10 px-3 py-2 text-sm" /></label></div><label className="block text-xs font-semibold">Ends at<input name="endsAt" type="datetime-local" className="mt-1 w-full rounded border border-black/10 px-3 py-2 text-sm" /></label><button disabled={pending} className="techpack-button w-full" type="submit">{pending ? "Creating…" : "Create discount code"}</button>{state.status !== "idle" ? <p className={`text-xs ${state.status === "error" ? "text-red-700" : "text-emerald-700"}`}>{state.message}</p> : null}</form>; }
