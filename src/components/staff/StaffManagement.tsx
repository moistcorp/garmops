import TechpackPageHeader from "@/components/portal/TechpackPageHeader";
import { requireStaffPermission } from "@/lib/auth/guards";
export default async function StaffManagement() { await requireStaffPermission("manage_staff"); return <div className="space-y-5"><TechpackPageHeader eyebrow="Founder" reference="Medusa staff provisioning" title="Staff management" description="Staff accounts are manually provisioned with the Medusa backend CLI. There is no public invitation or signup flow."/><section className="techpack-surface rounded border p-6 text-sm text-black/60">Use <code>npm run staff:create</code> in the backend workspace to provision Founder and Operations accounts.</section></div>; }
